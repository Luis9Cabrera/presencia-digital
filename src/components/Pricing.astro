---
// src/components/Pricing.astro
// src/components/Pricing.astro
const packages = [
    {
        name: "Básico",
        bad: "$900 – $1,200",
        features: [
            "Alta o reclamación de Google Maps",
            "Configuración correcta del negocio",
            "Horarios, dirección y teléfono",
            "Categoría correcta del negocio",
            "3–5 fotos del local",
            "Enlace directo a WhatsApp o teléfono",
        ],
        popular: false,
        color: "blue",
        icon: "📍",
    },
    {
        name: "Profesional",
        bad: "$1,800 – $2,500",
        features: [
            "Todo lo del plan Básico",
            "Optimización avanzada de Google Maps",
            "Descripción optimizada para búsquedas",
            "Configuración de WhatsApp Business",
            "Mensaje automático y botones",
            "Carga inicial de productos o servicios",
            "Guía para pedir reseñas a clientes",
        ],
        popular: true,
        color: "emerald",
        icon: "🚀",
    },
    {
        name: "Completo",
        bad: "$3,500",
        features: [
            "Todo lo del plan Profesional",
            "Optimización completa del perfil",
            "10–15 fotos optimizadas (local, productos, equipo)",
            "Publicaciones iniciales en Google",
            "Optimización básica de Facebook o Instagram",
            "Integración Maps + redes sociales",
            "Soporte y ajustes durante 30 días",
        ],
        popular: false,
        color: "purple",
        icon: "⭐",
    },
];
---

<section
    id="paquetes"
    class="relative py-24 bg-linear-to-b from-white to-slate-50 overflow-hidden"
>
    {/* Elementos decorativos de fondo */}
    <div
        class="absolute top-0 left-0 w-full h-1 bg-linear-to-r from-transparent via-blue-500 to-transparent opacity-20"
    >
    </div>
    <div
        class="absolute top-20 right-10 w-64 h-64 bg-blue-500/5 rounded-full blur-3xl"
    >
    </div>
    <div
        class="absolute bottom-20 left-10 w-80 h-80 bg-emerald-500/5 rounded-full blur-3xl"
    >
    </div>

    <div class="relative max-w-7xl mx-auto px-6 z-10">
        {/* Encabezado */}
        <div class="text-center mb-16">
            <span
                class="inline-block text-sm font-semibold text-blue-600 bg-blue-50 px-4 py-2 rounded-full mb-4"
            >
                Inversión Única - 0 Mensualidades
            </span>
            <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Elige el plan perfecto para tu <span class="text-blue-600"
                    >negocio</span
                >
            </h2>
            <p class="text-lg text-gray-600 max-w-3xl mx-auto">
                Precios transparentes. Pagas una sola vez y el perfil es tuyo
                para siempre.
            </p>
        </div>

        {/* Paquetes */}
        <div class="grid lg:grid-cols-3 gap-8">
            {
                packages.map((pkg, index) => {
                    const colorClasses = {
                        blue: {
                            bg: "from-blue-50 to-blue-100/50",
                            border: "border-blue-200",
                            text: "text-blue-700",
                            badge: "bg-blue-500 text-white",
                            price: "text-blue-900",
                        },
                        emerald: {
                            bg: "from-emerald-50 to-emerald-100/50",
                            border: "border-emerald-200",
                            text: "text-emerald-700",
                            badge: "bg-gradient-to-r from-emerald-500 to-green-500 text-white",
                            price: "text-emerald-900",
                        },
                        purple: {
                            bg: "from-purple-50 to-purple-100/50",
                            border: "border-purple-200",
                            text: "text-purple-700",
                            badge: "bg-purple-500 text-white",
                            price: "text-purple-900",
                        },
                    }[pkg.color];

                    return (
                        <div
                            class={`relative group ${pkg.popular ? "lg:-mt-4 lg:mb-4" : ""}`}
                        >
                            {/* Tarjeta del paquete */}
                            <div
                                class={`h-full bg-linear-to-br ${colorClasses.bg} border ${colorClasses.border} rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 overflow-hidden flex flex-col`}
                            >
                                {/* Encabezado del paquete */}
                                <div class="p-8 pb-6 grow">
                                    <div class="flex items-center justify-between mb-6">
                                        <div>
                                            <div class="text-4xl mb-2">
                                                {pkg.icon}
                                            </div>
                                            <h3 class="text-2xl font-bold text-gray-900">
                                                {pkg.name}
                                            </h3>
                                        </div>
                                        {pkg.popular && (
                                            <div
                                                class={`${colorClasses.badge} px-4 py-2 rounded-full text-sm font-bold animate-pulse`}
                                            >
                                                MÁS PEDIDO
                                            </div>
                                        )}
                                    </div>

                                    {/* Lista de características */}
                                    <ul class="space-y-4 mb-8">
                                        {pkg.features.map((feature, idx) => (
                                            <li class="flex items-start gap-3">
                                                <div
                                                    class={`w-6 h-6 ${colorClasses.text} rounded-full flex items-center justify-center shrink-0 mt-0.5`}
                                                >
                                                    <svg
                                                        class="w-4 h-4"
                                                        fill="currentColor"
                                                        viewBox="0 0 20 20"
                                                    >
                                                        <path
                                                            fill-rule="evenodd"
                                                            d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                                                            clip-rule="evenodd"
                                                        />
                                                    </svg>
                                                </div>
                                                <span class="text-gray-700">
                                                    {feature}
                                                </span>
                                            </li>
                                        ))}
                                    </ul>
                                </div>

                                {/* Sección de precios */}
                                <div class="bg-white/60 backdrop-blur-sm border-t border-slate-200 p-8">
                                    <div class="mb-6 text-center">
                                        <p class="text-gray-500 mb-1 text-sm uppercase tracking-wide font-semibold">
                                            Inversión Total
                                        </p>
                                        <div class="flex items-center justify-center gap-1">
                                            <span
                                                class={`text-4xl font-black ${colorClasses.price}`}
                                            >
                                                {pkg.bad}{" "}
                                                {/* Usando 'bad' como precio real por ahora, ajustar datos si necesario */}
                                            </span>
                                            <span class="text-gray-500 font-medium self-end mb-1">
                                                MXN
                                            </span>
                                        </div>
                                        <p class="text-xs text-gray-400 mt-2">
                                            Pago único • Sin rentas mensuales
                                        </p>
                                    </div>

                                    {/* Botón de acción */}
                                    <a
                                        href={`https://wa.me/525649646558?text=Hola, me interesa el paquete ${pkg.name}`}
                                        class={`block w-full text-center py-4 px-6 rounded-xl font-bold transition-all duration-300 transform hover:scale-[1.02] ${
                                            pkg.popular
                                                ? "bg-linear-to-r from-emerald-600 to-green-600 hover:from-emerald-700 hover:to-green-700 text-white shadow-lg hover:shadow-xl"
                                                : "bg-white text-gray-800 border-2 border-slate-300 hover:border-blue-500 hover:bg-blue-50"
                                        }`}
                                    >
                                        {pkg.popular
                                            ? "¡Lo quiero!"
                                            : "Más información"}
                                    </a>
                                </div>
                            </div>
                        </div>
                    );
                })
            }
        </div>

        {/* Garantía */}
        <div
            class="mt-20 max-w-3xl mx-auto bg-white rounded-2xl p-8 border border-slate-200 shadow-sm flex flex-col md:flex-row items-center gap-6 text-center md:text-left"
        >
            <div class="shrink-0">
                <div
                    class="w-20 h-20 bg-blue-100 rounded-full flex items-center justify-center"
                >
                    <svg
                        class="w-10 h-10 text-blue-600"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                        ><path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                        ></path></svg
                    >
                </div>
            </div>
            <div>
                <h3 class="text-xl font-bold text-gray-900 mb-2">
                    Garantía de Satisfacción
                </h3>
                <p class="text-gray-600">
                    Tu inversión está segura. Pagas el 50% al iniciar y el 50%
                    restante SOLO cuando veas tu perfil publicado y optimizado
                    en Google Maps.
                </p>
            </div>
        </div>
    </div>
</section>
